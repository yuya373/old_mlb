<h1>Listing data</h1>


<table>
  <thead>
    <tr>
<!--       <th>Gid</th>
 -->      <th>Year</th>
      <th>Month</th>
      <th>Day</th>
<!--       <th>Team name</th>
      <th>Team</th>
 -->      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @data.each do |datum| %>
    <tr>
<!--       <td><%= datum.gid %></td>
 -->      <td><%= datum.year %></td>
      <td><%= datum.month %></td>
      <td><%= datum.day %></td>
<!--       <td><%= datum.team_name %></td>
      <td><%= datum.team_id %></td>
 -->      <td><%= link_to 'Show', datum %></td>
      <td><%= link_to 'Edit', edit_datum_path(datum) %></td>
      <td><%= link_to 'Destroy', datum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br />

<%= link_to 'New Datum', new_datum_path %>
