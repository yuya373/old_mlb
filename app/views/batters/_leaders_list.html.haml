-@leaders.each do |k,v|
  .hero-unit.span3
    %button.btn.btn-mini.pull-right{'data-toggle' => 'collapse', 'data-target' => "##{k}"} more
    %h3= k.to_s.upcase
    -v[0..2].each do |batter|
      %p
        =link_to "#{batter.name_display_roster}", {controller: :batters, action: :show, p_id: batter.p_id}
        %small="- #{batter[k.to_sym]}"
    .collapse{id: "#{k}"}
      -v[3..9].each do |batter|
        %p
        =link_to "#{batter.name_display_roster}", {controller: :batters, action: :show, p_id: batter.p_id}
        %small="- #{batter[k.to_sym]}"

